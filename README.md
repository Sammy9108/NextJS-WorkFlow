# NextJS-WorkFlow

Cache - in GitHub Workflow catches the node installations that helps in next run

Artifact =  a saved copy of build output that GitHub actions stores so we can download and use it later jobs like for our deployment steps (here available in out/ path)
